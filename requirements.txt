--extra-index-url https://download.pytorch.org/whl/cu124

# setuptools 81+ REMOVEU o pkg_resources, que o chatterbox (via resemble-perth)
# ainda importa → sem este pin a narração quebra com "'NoneType' object is not callable".
setuptools<81

# Web
fastapi
uvicorn[standard]
python-multipart
websockets
httpx  # buscar/baixar livros grátis (Gutenberg, Wikisource, Internet Archive)

# App de desktop: abre a UI numa janela nativa (WebView2) em vez do navegador.
pywebview

# PDF
PyMuPDF==1.24.1
pyphen==0.15.0
nltk==3.8.1

# Motor TTS: Chatterbox (Resemble AI) — licença MIT, wheel pré-compilado (py3-none-any),
# instala SEM o compilador C++ da Microsoft. Voz nativa pt-BR via modelo multilíngue.
# Motor de narração: XTTS-v2 via coqui-tts (fork mantido idiap). ~2GB VRAM,
# estável em placa lotada, vozes pt-BR embutidas. Um shim em runtime repõe
# isin_mps_friendly para rodar com transformers 5.x.
coqui-tts==0.27.5
# Chatterbox foi aposentado (usava ~5.6GB de VRAM e travava). O código do engine
# continua no repo como fallback opcional, mas NÃO é instalado por padrão. Para
# habilitá-lo: pip install chatterbox-tts==0.1.7 e LEIA_TTS_ENGINE=chatterbox.
torch==2.6.0+cu124
torchaudio==2.6.0+cu124

numpy==1.26.4
